# eCommerce-app

A Simple eCommerce App made with Laravel and Vue.

 1. Start by running `npm run install`to install the neccessary package.
 2. Next run `npm run build` to build to app
 3. Lastly, run `php artisan serve` to run the server.